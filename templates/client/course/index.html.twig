{% extends 'client/base-client.html.twig' %}

{% block content %}
    <div class="container">
        <h2>{{ course.module.label }} - {{ course.title }}</h2>
        <hr/>
        <div>
            <p>{{ course.content }}</p>
        </div>
    </div>
{% endblock %}
