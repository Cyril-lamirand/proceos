<form method="post" action="{{ path('exercice_delete', {'id': exercice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice.id) }}">
    <button class="btn">Delete</button>
</form>
