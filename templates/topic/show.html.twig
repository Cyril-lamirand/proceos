{% extends 'base.html.twig' %}

{% block title %}Topic{% endblock %}

{% block body %}
    <h1>Topic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ topic.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ topic.title }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ topic.createdat ? topic.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('topic_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('topic_edit', {'id': topic.id}) }}"><i class="fas fa-pen"></i></a>

    {{ include('topic/_delete_form.html.twig') }}
{% endblock %}
