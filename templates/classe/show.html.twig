{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ classe.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('classe_index') }}">back to list</a>

    <a class="btn btn-success" href="{{ path('classe_edit', {'id': classe.id}) }}"><i class="fas fa-pen"></i></a>

    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
