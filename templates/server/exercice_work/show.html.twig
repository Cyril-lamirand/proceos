{% extends './server/base.html.twig' %}

{% block title %}ExerciceWork{% endblock %}

{% block body %}
    <h1 class="big-titles">ExerciceWork</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice_work.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ exercice_work.content }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ exercice_work.link }}</td>
            </tr>
            <tr>
                <th>Createdat</th>
                <td>{{ exercice_work.createdat ? exercice_work.createdat|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn back-button" href="{{ path('exercice_work_index') }}">back to list</a>

    <a class="btn update-button" href="{{ path('exercice_work_edit', {'id': exercice_work.id}) }}"><i class="fas fa-pen"></i></a>

    {{ include('server/exercice_work/_delete_form.html.twig') }}
{% endblock %}
