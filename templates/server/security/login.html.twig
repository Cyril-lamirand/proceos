<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Proceos | Authentification{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}
        <style>
            body{ background-color: #202A46; }
            .card-auth{ border: 2px solid #7367f0; border-radius: 16px; padding-left: 10px; padding-right: 10px; color:white; background: #161d31; }
            .color-proceos-1{background: #7367f0; border:none;}
            span{ color: white; }
            a{ color: #7367f0; }
        </style>
    </head>
    <body>
        <header>

        </header>
        <main>
            <form method="post">
                <div class="container">
                    <div class="row">
                        <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                            <!-- Keep it empty -->
                        </div>
                        <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                            <div class="shadow card-auth pt-5 pb-5 mt-5">
                                <div>

                                </div>
                                <div class="text-center">
                                    <h2>Authentification</h2>
                                </div>
                                <hr/>
                                <div>
                                    <label for="inputEmail">Adresse email</label>
                                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
                                </div>
                                <div class="mt-3">
                                    <label for="inputPassword">Mot de passe</label>
                                    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                                </div>
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                <hr/>
                                <div class="text-center pt-4">
                                    <button class="btn btn-primary color-proceos-1 w-100" type="submit">
                                        Connexion
                                    </button>
                                </div>
                            </div>
                            {% if error %}
                                <div class="alert alert-danger mt-4">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}
                            {% if app.user %}
                                <div class="mb-3">
                                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                                </div>
                            {% endif %}
                            <div class="col-12 mt-3">
                                <span>Pas encore inscrit ? <a href="/register">Inscrivez-vous</a> !</span>
                            </div>
                        </div>
                        <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                            <!-- Keep it empty -->
                        </div>
                    </div>
                </div>
                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>
                #}
            </form>
        </main>
        <footer>

        </footer>
    </body>
</html>
