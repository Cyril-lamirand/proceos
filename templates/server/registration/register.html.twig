<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Proceos | Inscription{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <link rel="stylesheet" href="{{asset('css/index-client.css')}}">
        {% block stylesheets %}
        {% endblock %}
        <style>
            body{ background-color: #202A46; }
            .card-auth{ border: 2px solid #7367f0; border-radius: 16px; padding-left: 10px; padding-right: 10px; color:white; background: #161d31; z-index: 3; }
            .color-proceos-1{background: #7367f0; border:none; color: white;}
            span{ color: white; }
            a{ color: #7367f0; }
            .white{ color: white; }
            .nav-proceos{ background:#161d31; border-bottom:2px solid #7367f0; }
        </style>
    </head>
    <body>

        <header>
            <nav class="navbar navbar-expand-lg bg-dark nav-proceos fixed-top">
                <div class="container-fluid">
                    <a class="navbar-brand text-white" href="/">Proceos</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNavDropdown">

                        <div class="w-100" style="text-align: right">
                            <span class="text-white-50">{% if app.user %}Role : {{ app.user.roles[0] }} {% else %} <a href="/login">Connexion</a>{% endif %}</span>
                        </div>

                    </div>
                </div>
            </nav>
        </header>
        <main class="container mt-5">
            {% if app.user %}
            <div class="mt-5 pt-5">
                <div class="text-center">

                </div>
                <div class="text-center">
                    <h3 class="white">Hey {{ app.user.firstname }}, il semblerait que vous soyez déjà connecté ! </h3>
                </div>
                <p class="white">On te propose de te rendre ici :</p>
                <ul class="white">
                    <li><a href="/">Page d'accueil</a></li>
                    <li><a href="/dashboard">Ton tableau de bord</a></li>
                    <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
                </ul>
            </div>
            {% else %}
            <form method="post">
                <div class="row">
                    <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                        <!-- Keep it empty -->
                    </div>
                    <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                        <div class="shadow card-auth pt-5 pb-5 mt-5">
                            <div>

                            </div>
                            <div class="text-center">
                                <h2>Inscription</h2>
                            </div>
                            <hr/>
                            {{ form_start(registrationForm) }}
                            <div>
                                {{ form_label(registrationForm.firstname, null, { 'label': 'Prénom'}) }}
                                {{ form_widget(registrationForm.firstname, {
                                    'attr': {
                                        'class': 'form-control',
                                        'placeholder': 'Ex : Lionel...'
                                    }})
                                }}
                            </div>
                            <div class="mt-3">
                                {{ form_label(registrationForm.lastname, null, { 'label': 'Nom'}) }}
                                {{ form_widget(registrationForm.lastname, {
                                    'attr': {
                                        'class': 'form-control',
                                        'placeholder': 'Ex : Messi...'
                                    }})
                                }}
                            </div>
                            <div class="mt-3">
                                {{ form_label(registrationForm.email, null, { 'label': 'Adresse email'}) }}
                                {{ form_widget(registrationForm.email, {
                                    'attr': {
                                        'class': 'form-control',
                                        'placeholder': 'Ex : lionel.messi@edu.devinci.fr'
                                    }})
                                }}
                            </div>
                            <div class="mt-3">
                                {{ form_label(registrationForm.plainPassword, null, { 'label': 'Mot de passe'}) }}
                                {{ form_widget(registrationForm.plainPassword, {
                                    'attr': {
                                        'class': 'form-control',
                                        'placeholder': '6 charactères minimum...'
                                    }})
                                }}
                            </div>
                            <div class="mt-3">
                                {{ form_label(registrationForm.agreeTerms, null, { 'label': "Conditions générales d'utilisation"}) }}
                                {{ form_widget(registrationForm.agreeTerms, {
                                    'attr': {
                                        'class': 'form-checkbox'
                                    }})
                                }}
                            </div>
                            <div class="mt-4">
                                <button type="submit" class="btn color-proceos-1 w-100">S'inscrire</button>
                            </div>
                            {{ form_end(registrationForm) }}
                        </div>
                        <div class="col-12 mt-3">
                            <span>Déjà inscrit ? <a href="/login">Connectez-vous</a> !</span>
                        </div>
                        {% if app.user %}
                            <div class="mb-3">
                                You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                            </div>
                        {% endif %}
                    </div>
                    <div class="col-12 col-sm-12 col-md-3 col-lg-4 col-xl-4">
                        <!-- Keep it empty -->
                    </div>
                </div>
                {#
                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                See https://symfony.com/doc/current/security/remember_me.html

                <div class="checkbox mb-3">
                    <label>
                        <input type="checkbox" name="_remember_me"> Remember me
                    </label>
                </div>
                #}
            </form>
            {% endif %}
        </main>
        <footer>

        </footer>
        {% include "/client/layouts/particles.html.twig" %}
    </body>
</html>
